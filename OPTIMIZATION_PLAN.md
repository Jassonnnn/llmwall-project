# jb_demo 优化与扩展执行计划（2026Q2）

## 1. 目标

- 把当前原型升级为可持续运行的评估服务。
- 支持“红队评估 + 安全护栏”统一接入，提供稳定后端接口。
- 提升评估可信度（分类质量、判题质量、可复现性）。
- 建立基本工程化能力（测试、CI、可观测性、回滚）。

## 2. 范围与优先级

### P0（上线前必须）

1. 接口安全：为关键接口增加认证与权限控制。  
2. 密钥治理：API Key 服务端安全存储，前端不传明文。  
3. 资源保护：限流、并发配额、超时与取消任务。  
4. 可靠性：模型调用重试/退避/熔断，错误码与状态码统一。  
5. 评估可信度：`llm_judge` 结构化判题、关键词规则校准。  
6. 数据分类质量：生产强制 `v2` 索引，低置信/冲突样本复核闭环。  
7. 质量保障：最小测试集 + CI 自动检查。

### P1（1-2 周增强）

1. 统一评估 API：红队 + 护栏统一任务协议。  
2. 任务化执行：创建任务/查询状态/拉取结果/取消任务。  
3. 结果持久化：任务、配置、样本、结果入库。  
4. 报告能力：CSV/JSON/Markdown 导出与基线对比。  
5. 前端增强：任务历史、重跑、筛选、排序、质量指标展示。

### P2（平台化扩展）

1. 攻击方法插件化与评估器插件化。  
2. 定时回归与趋势分析（周报/月报）。  
3. 多护栏适配器（可插拔第三方或自研护栏）。  
4. 部署标准化（Docker/Compose、`.env.example`、一键启动）。

## 3. 里程碑计划

## M1（第 1 周）：安全与可用性兜底

- 完成关键接口鉴权（`/api/config`、`/api/generate_attacks`、`/api/batch_evaluate`、`/api/attack_and_evaluate`）。
- 改造密钥流：只在后端保存，前端使用 `has_api_key` 状态，不回传原始 key。
- 增加批量任务取消能力与并发限制。
- 统一错误响应结构：`code/message/details/http_status`。

**验收标准**
- 未认证请求访问关键接口返回 401/403。
- 无法在前端或日志中看到明文密钥。
- 批量任务可中途取消，且服务无僵尸任务。

## M2（第 2 周）：评估可信度提升

- `llm_judge` 改为 JSON 输出协议，避免字符串硬匹配。
- 关键词评估器加入版本化规则与回归样本。
- 分类索引策略升级：默认要求 v2，v1 仅作为临时开关回退。
- 新增分类质量复核脚本与抽检结果回写。

**验收标准**
- 判题误判率相对当前基线下降（目标 >= 20%）。
- `/api/datasets` 返回稳定质量指标（coverage/conflict/low_confidence）。
- 索引构建流程可复现（固定 seed + 同版本参数）。

## M3（第 3 周）：统一接口与任务化

- 提供统一评估入口（支持 red-team + guardrail 组合评估）。
- 增加任务生命周期接口：创建、查询、取消、结果分页。
- 结果持久化（建议 SQLite 起步，后续可切 PostgreSQL）。

**验收标准**
- 外部前端只调用统一接口即可完整跑通一次评估。
- 服务重启后任务与结果可追溯。

## M4（第 4 周）：工程化与交付

- 补齐单元测试/接口冒烟测试。
- 接入 CI（lint + test + build checks）。
- 输出部署文档、运行手册、回滚手册。

**验收标准**
- 新提交自动跑 CI，失败可阻断合并。
- 新同学按文档可在 30 分钟内完成本地启动与一次评估。

## 4. 本周建议执行顺序（可直接开工）

1. 先做 P0-1/P0-2（鉴权 + 密钥治理）。
2. 再做 P0-3（限流、并发、取消任务）。
3. 紧接 P0-4/P0-5（错误码统一 + 判题结构化）。
4. 收尾做 P0-6/P0-7（索引质量闭环 + 测试/CI）。

## 5. 风险与依赖

- 依赖第三方模型接口稳定性与配额。  
- 真实攻击算法（如 GCG/AutoDAN）对本地算力与模型依赖较高。  
- 若需要多租户或团队协作，需尽早确定鉴权方案（JWT/API Token/网关）。

## 6. 成功指标（KPI）

- 稳定性：批量评估成功完成率 >= 99%。
- 质量：分类冲突率与低置信度比例持续下降。
- 安全：0 次密钥明文泄露事件。
- 效率：同规模任务平均耗时下降 >= 30%。
- 可维护性：关键路径测试覆盖率达到目标阈值（先 60%，再提升）。
