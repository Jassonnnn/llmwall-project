# `jb_demo` 每周汇报话术（第 2 周 - 第 12 周）

适用场景：
- 组会周报
- 给师兄/老师做阶段汇报
- 按“每周一个明确产出”来介绍项目推进

## 第 2 周：数据集分类与批量评估改造

本周我们主要解决的是“数据集太杂、前端没法按类型选择测试”的问题。  
我先把现有有害数据集按越狱攻击方式做了分类，增加了像角色扮演、编码混淆、上下文注入、多语言绕过、组合攻击，以及一个“混合全部”聚合分类。  
在这个基础上，批量评估流程也做了调整，改成“先按攻击分类筛选，再按固定条数评测”，这样前端就可以选择要测哪一类攻击、测多少条数据。  
这周的核心产出是：数据集从“原始堆放”变成了“可按攻击类型组织和评估”的状态，为后面更精细的测试打了基础。

## 第 3 周：第一批真实攻击方法接入

这一周的重点是把原来偏模拟的攻击方法，开始替换成真实攻击链路。  
我优先接入了 `PAIR`、`TAP`、`GCG`、`AutoDAN` 这四个比较核心的方法，并把真实 attacker 的调用链、依赖配置和异常回退逻辑打通。  
也就是说，这一阶段不再只是“生成一个看起来像攻击的提示词”，而是尽量按照真实论文方法或框架实现去跑。  
这周的核心产出是：第一批核心攻击方法从模拟模式升级为真实链路，项目的攻击真实性明显提升。

## 第 4 周：第二批真实攻击方法接入

在第一批方法跑通之后，我继续把其余 EasyJailbreak 体系里的主要方法补齐。  
这一周接入了 `Cipher`、`JailBroken`、`DeepInception`、`MultiLingual`、`CodeChameleon`、`GPTFuzz`、`ReNeLLM`、`ICA`。  
除了接方法本身，我还统一整理了这些方法的状态说明、能力边界，以及配置不足时的错误回退逻辑，避免外部看起来“都支持”，但实际上很多是伪成功。  
这周的核心产出是：EasyJailbreak 相关真实方法基本补齐，项目从“部分真实攻击”推进到“主流方法基本覆盖”。

## 第 5 周：评估可信度与安全治理增强

这一周我没有继续堆新攻击方法，而是重点提升系统本身的可信度和稳定性。  
首先把 `llm_judge` 改成了结构化 JSON 判题，避免原来靠文本解析带来的误判问题；同时把关键词规则做了版本化，便于后续回归和溯源。  
其次，分类索引升级到了 `v2`，并增加了人工复核回写机制，让低置信度和冲突样本可以继续纠正。  
另外，在系统层面补上了统一错误协议、接口鉴权、任务取消、并发限制这些治理能力。  
这周的核心产出是：项目从“能跑”提升到了“更可信、更稳、更适合长期使用”的状态。

## 第 6 周：统一任务接口与前端适配

最后这一周，重点是把项目从一个内部原型，推进成一个可以被外部前端接入的后端服务。  
我新增了统一任务 API，支持任务创建、状态查询、结果分页、任务取消，并用 SQLite 做了任务和结果持久化。  
在这个基础上，又增加了和 `safetydash` 对接需要的兼容接口，包括 `POST /api/auth/login`、`GET /api/auth/me` 和 `GET /api/redteam/overview`。  
同时把相关文档和联调说明补齐，方便前端直接接入。  
这周的核心产出是：项目具备了对接外部前端的后端能力，不再只是一个本地演示原型。

## 第 7 周：多攻击方法横向评估能力

这一周的重点，不是继续单独加方法，而是把“评估任务本身”升级成支持多方法对比。  
我把统一评估接口改成了可以一次提交多种攻击方法，比如同一批种子样本上同时跑 `PAIR`、`TAP` 这类方法，而不是一次只能测一种。  
同时增加了“每个种子样本生成多少条攻击提示词”的控制项，这样就可以更系统地比较不同方法在同一数据上的表现差异。  
这周的核心产出是：系统从“单方法单次评估”升级成了“多攻击方法横评”的形态，更适合做方法对比实验。

## 第 8 周：多维指标与分类聚合

在支持多方法横评之后，这一周重点是把结果统计做细。  
我补上了任务级总体指标，包括攻击成功率、拒答率、错误率、平均时延，同时新增了按攻击方法、按攻击分类的聚合统计。  
也就是说，现在不只是能看到“总共成功多少条”，还可以拆开看“哪一种方法更强”“哪一类有害数据更容易打穿”。  
这周的核心产出是：评估结果从“一个总数”变成了“可分层分析的多维指标体系”。

## 第 9 周：方法排行榜与分类对比输出

这一周我继续把聚合结果往“更适合汇报和展示”的方向整理。  
具体来说，我把按攻击方法、按攻击分类的统计结果做了统一排序和排名，这样前端或者汇报材料里可以直接展示方法排行榜、分类对比表。  
这一改动的价值在于，评估结果不再只是后台存一堆明细，而是已经具备了直接拿来做横向比较和结果展示的基础结构。  
这周的核心产出是：系统已经能直接输出“方法对比”和“分类对比”的结果骨架。

## 第 10 周：评估复现性与过程留痕

这一周的重点是解决“结果出来了，但后面不好复盘”的问题。  
我在结果明细里补充记录了原始种子提示词、来源攻击分类、具体攻击方法、生成模式等字段，让每一条结果都能追溯回“它是怎么来的”。  
另外，任务层面也保留了配置快照，方便后面排查到底是模型配置、攻击方法还是评估器设置导致了结果差异。  
这周的核心产出是：评估系统不仅能给结果，还能把结果的来龙去脉留痕下来，便于复现和审计。

## 第 11 周：评估报告导出与收口

最后这一周，重点是把前面的评估能力真正收口成可以交付和汇报的结果。  
我补上了统一导出接口，支持把任务结果直接导出成 `json`、`csv`、`markdown` 三种格式，既方便程序处理，也方便直接拿去写周报或展示。  
同时也补了针对这套能力的后端测试，覆盖了旧数据库升级、多方法评估、聚合统计和导出链路，确保这次增强不是“看起来有”，而是真的能跑。  
这周的核心产出是：评估能力从“后台可用”推进到了“可导出、可复盘、可直接汇报”的状态。

## 第 12 周：真实攻击链稳定性收尾与全量验证

这一周的重点，是把已经接入的真实攻击方法真正跑稳，而不是继续只加方法名。  
我集中修复了 `PAIR`、`TAP`、`GCG`、`AutoDAN` 这几个最容易因为模型抖动、白盒模板别名、资源依赖或参数兼容而失败的方法，把它们统一收敛到可稳定执行的真实攻击链。  
同时补了运行时兼容修复，包括 OpenAI 兼容消息格式处理、白盒生成参数合并、AutoDAN 的 `NLTK` 资源检查与生成告警清理，以及 FastAPI 生命周期改造，减少无意义 warning。  
最后做了一轮完整回归：12 个真实攻击方法全部返回成功，`compileall` 通过，`pytest -q tests/test_evaluation_tasks.py` 也通过。  
这周的核心产出是：项目从“真实攻击基本接上”进一步提升到了“真实攻击整体可跑、可验证、可汇报”的状态。

## 一句话总结版

这十一周的推进逻辑可以概括为：  
前半段先把数据集组织方式、真实攻击方法、系统可信度和统一接口做起来；后半段再把评估系统升级成支持多方法对比、多维指标分析、结果留痕和报告导出的形态。  
最后再把真实攻击链和运行稳定性做收口。  
也就是从“能演示”逐步推进到“能测试、能治理、能对接、也能拿来做横向评估和正式汇报”的状态。
