# `jb_demo` 每周汇报话术（第 2 周 - 第 6 周）

适用场景：
- 组会周报
- 给师兄/老师做阶段汇报
- 按“每周一个明确产出”来介绍项目推进

## 第 2 周：数据集分类与批量评估改造

本周我们主要解决的是“数据集太杂、前端没法按类型选择测试”的问题。  
我先把现有有害数据集按越狱攻击方式做了分类，增加了像角色扮演、编码混淆、上下文注入、多语言绕过、组合攻击，以及一个“混合全部”聚合分类。  
在这个基础上，批量评估流程也做了调整，改成“先按攻击分类筛选，再按固定条数评测”，这样前端就可以选择要测哪一类攻击、测多少条数据。  
这周的核心产出是：数据集从“原始堆放”变成了“可按攻击类型组织和评估”的状态，为后面更精细的测试打了基础。

## 第 3 周：第一批真实攻击方法接入

这一周的重点是把原来偏模拟的攻击方法，开始替换成真实攻击链路。  
我优先接入了 `PAIR`、`TAP`、`GCG`、`AutoDAN` 这四个比较核心的方法，并把真实 attacker 的调用链、依赖配置和异常回退逻辑打通。  
也就是说，这一阶段不再只是“生成一个看起来像攻击的提示词”，而是尽量按照真实论文方法或框架实现去跑。  
这周的核心产出是：第一批核心攻击方法从模拟模式升级为真实链路，项目的攻击真实性明显提升。

## 第 4 周：第二批真实攻击方法接入

在第一批方法跑通之后，我继续把其余 EasyJailbreak 体系里的主要方法补齐。  
这一周接入了 `Cipher`、`JailBroken`、`DeepInception`、`MultiLingual`、`CodeChameleon`、`GPTFuzz`、`ReNeLLM`、`ICA`。  
除了接方法本身，我还统一整理了这些方法的状态说明、能力边界，以及配置不足时的错误回退逻辑，避免外部看起来“都支持”，但实际上很多是伪成功。  
这周的核心产出是：EasyJailbreak 相关真实方法基本补齐，项目从“部分真实攻击”推进到“主流方法基本覆盖”。

## 第 5 周：评估可信度与安全治理增强

这一周我没有继续堆新攻击方法，而是重点提升系统本身的可信度和稳定性。  
首先把 `llm_judge` 改成了结构化 JSON 判题，避免原来靠文本解析带来的误判问题；同时把关键词规则做了版本化，便于后续回归和溯源。  
其次，分类索引升级到了 `v2`，并增加了人工复核回写机制，让低置信度和冲突样本可以继续纠正。  
另外，在系统层面补上了统一错误协议、接口鉴权、任务取消、并发限制这些治理能力。  
这周的核心产出是：项目从“能跑”提升到了“更可信、更稳、更适合长期使用”的状态。

## 第 6 周：统一任务接口与前端适配

最后这一周，重点是把项目从一个内部原型，推进成一个可以被外部前端接入的后端服务。  
我新增了统一任务 API，支持任务创建、状态查询、结果分页、任务取消，并用 SQLite 做了任务和结果持久化。  
在这个基础上，又增加了和 `safetydash` 对接需要的兼容接口，包括 `POST /api/auth/login`、`GET /api/auth/me` 和 `GET /api/redteam/overview`。  
同时把相关文档和联调说明补齐，方便前端直接接入。  
这周的核心产出是：项目具备了对接外部前端的后端能力，不再只是一个本地演示原型。

## 一句话总结版

这五周的推进逻辑可以概括为：  
先把数据集和评估组织方式理顺，再逐步把攻击方法真实化，接着补齐评估可信度和系统治理能力，最后统一后端接口并完成前端适配。  
也就是从“能演示”逐步推进到“能测试、能治理、能对接”的状态。
